<div class="container-fluid mt-5 pt-5">
  <div class="row d-flex justify-content-center">
      <div class="col-10 col-md-8 title-page mb-3 d-block d-sm-block d-md-none">
        <div class="row">
          <div class="col-12 col-md-6 d-flex justify-content-center justify-content-md-start align-items-center">
            <h2>Find your match!</h2>
          </div>
          <div class="col-12 col-md-6 d-flex justify-content-around align-items-center">
            <button type="button" class="btns-matchs btn btn-outline-success waves-effect btn-sm" onclick="createNewMatch()">Create Match</button>
            <button type="button" class="btns-matchs btn btn-outline-light btn-sm" onclick="showMyMatches()">My Matches</button>
        </div>
      </div>
    </div>
    <div class="col-10 pb-5 mt-3">
      <div class="row d-flex justify-content-around">
        {{#each matches}}
          {{> matchCard this}} 
        {{/each}}
        {{#each matches}}
          {{> matchCard this}} 
        {{/each}}
      </div>
    </div>

  </div>
</div>